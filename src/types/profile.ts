export interface ProfileFormData {
  name: string;
  bio: string;
  image: string;
  birthDate: string;
  currentPassword: string;
  newPassword: string;
  confirmPassword: string;
  email: string;
} 